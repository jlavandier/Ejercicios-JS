import * as moduloController from './controller.js'
import chalk from 'chalk';

const sum = moduloController.suma(1,2)
console.log(sum);

const mul = moduloController.multiplica(4,5)
console.log(chalk.green(mul));